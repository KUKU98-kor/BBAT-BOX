# BBAT BOX 테스트 서버 준비

## 현재 상태

- GitHub 저장소: `KUKU98-kor/BBAT-BOX`
- 개발 브랜치: `beta`
- 배포 대상: Vercel Preview
- 현재 화면과 기록 기능은 정적 웹 앱으로 바로 배포할 수 있습니다.
- 계정, 팀 권한, 일정, 라인업, 경기 기록은 아직 각 브라우저의 `localStorage`에 저장됩니다.

`localStorage` 버전은 한 기기에서 화면과 기록 흐름을 시험하기에는 충분하지만, 여러 사람이 같은 배트조짐 팀을 공유하는 테스트에는 사용할 수 없습니다. 다른 기기에서는 별도의 계정과 별도의 팀 데이터가 만들어지며, 실시간 LIVE도 서로 동기화되지 않습니다.

## 권장 테스트 구조

1. **Vercel Preview**가 `beta` 브랜치의 화면을 배포합니다.
2. **Supabase**가 계정, 팀, 권한, 일정, 라인업, 경기 기록과 LIVE 데이터를 저장합니다.
3. 최초 1회 서버에서 기존 소유자 계정에 `배트조짐 / host` 권한을 지정합니다.
4. 이후 가입자는 기본적으로 팀 미가입 상태이며, 호스트의 초대 또는 참가 코드로만 배트조짐에 들어옵니다.

호스트 아이디나 비밀번호는 저장소, 프런트엔드 코드, Vercel 환경 변수에 평문으로 넣지 않습니다. 비밀번호 검증은 인증 서버가 담당하고, 호스트 권한은 서버 데이터베이스의 팀 멤버십으로 확인해야 합니다.

## Vercel Preview 연결 방법

1. [Vercel](https://vercel.com/)에서 GitHub 계정으로 로그인합니다.
2. **Add New → Project**를 선택합니다.
3. `KUKU98-kor/BBAT-BOX` 저장소를 가져옵니다.
4. Framework Preset은 **Other**, Root Directory는 저장소 루트로 둡니다.
5. Deploy를 누릅니다.
6. Project Settings → Git에서 Preview Branch에 `beta`가 연결되는지 확인합니다.

연결 후에는 `beta` 브랜치에 푸시할 때마다 새로운 테스트 주소가 자동 생성됩니다. 최종 서비스가 준비되기 전까지 `main` 프로덕션 배포 대신 Preview 주소만 팀원에게 전달하는 편이 안전합니다.

## 서버 로그인 전환 순서

1. Supabase 프로젝트 생성
2. 사용자 프로필, 팀, 팀 멤버, 초대, 경기, 타석, 투구, 일정 테이블 생성
3. 서버 인증으로 회원가입·로그인 교체
4. 현재 로그인한 소유자 계정을 서버에 한 번 등록
5. `배트조짐` 팀 생성 후 해당 계정만 `host`로 지정
6. 기존 브라우저의 일정·라인업·경기 기록을 서버로 1회 이전
7. 호스트 초대 기능과 권한 검증 연결
8. 여러 기기에서 LIVE 동기화 시험

서버 연결이 완료되기 전에는 현재 로그인 화면을 보안이 보장된 실제 계정 시스템으로 간주하면 안 됩니다.

## 배포 전 확인

- `node --check app.js`
- `node --check scorebook.js`
- `git diff --check`
- 모바일과 아이패드 화면 확인
- 일반 사용자에게 팀·리그 편집 버튼이 보이지 않는지 확인
- 호스트만 초대, 선수 관리, 일정, 라인업, 기록 수정이 가능한지 확인
- 서로 다른 두 기기에서 같은 경기 LIVE가 갱신되는지 확인

